kind: Servive
apiVersion: v1
metadata:
  name: myservice
  labels:
    app: myspringbootapp
spec:
  selector:
      app: myspringbootapp
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 9099
      nodePort: 31200


